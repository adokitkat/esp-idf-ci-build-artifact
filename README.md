# ESP-IDF Build Binary Artifacts

This action builds ESP-IDF firmware (separate binaries + merged binary) and uploads it as artifacts.

Put it in `.github/workflows` folder and manually trigger from "Actions" page.
